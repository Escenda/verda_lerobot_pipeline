# Example .env (Git 管理外推奨) — ローカル/リモート双方で必要なシークレットを集約

# =====================
# Verda (DataCrunch) API auth
# =====================
# Required locally to create/cleanup instances via the SDK.
DATACRUNCH_CLIENT_ID="your-client-id"
DATACRUNCH_CLIENT_SECRET="your-client-secret"

# =====================
# Local defaults (optional)
# =====================
# Default SSH key names/paths you pass via CLI (optional)
#VERDA_SSH_KEY_NAME="your-verda-ssh-key-name"
#VERDA_SSH_PRIVATE_KEY="~/.ssh/id_ed25519"

# =====================
# Remote auth (secrets uploaded to the training VM)
# =====================
# Hugging Face の private dataset/model へのアクセス用
#HUGGINGFACE_HUB_TOKEN="hf_xxxxxxxxxxxxxxxxxxxxxxxxx"

# wandb credentials (project/entity are optional but convenient to keep here)
#WANDB_API_KEY="xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
#WANDB_PROJECT="lerobot-training"
#WANDB_ENTITY="your-wandb-entity"
#LEROBOT_WANDB_ENABLE="true"

# トレーニング設定は train.remote.yaml / train.remote.example.yaml を参照してください。
